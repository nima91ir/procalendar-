# AGENTS.md

The project is a Flutter app. Everything lives in `fitness_trainer_app/` —
read `fitness_trainer_app/AGENTS.md` for commands, conventions, and test
rules before changing anything.

The root of this workspace is NOT the app; run all Flutter commands with
working directory `fitness_trainer_app/`.